apiVersion: v1
kind: Service
metadata:
  name: mongodb-svc
  namespace: expense-tracker
spec:
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None          # headless service - required for StatefulSet